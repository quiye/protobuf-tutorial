syntax = "proto3";
package api;

message NestFormula {
  enum FormulaType {
    MONO_FORMULA = 0;
    POLY_FORMULA = 1;
  }
  string op_string = 1;
  repeated NestFormula poly = 2;
  int32 mono = 3;
  FormulaType type = 4;
}